#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
  
    int n,j;
    cin >> n;    
    vector<int> v;
    for(int i=0; i<n; i++){
           cin>> j;
        v.push_back(j);      
       }
    sort(v.begin(), v.end());
    
    for(int k=0; k< v.size(); k++){
        cout << v[k] << " "; 
    }
    
    return 0;
}
